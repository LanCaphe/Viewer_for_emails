$(document).ready(function() {

  let selected_email = $("<%= j (render 'layouts/select') %>").hide();
  $('.emails-body').empty();
  $('.emails-body').append(selected_email);
  selected_email.fadeIn();
});
